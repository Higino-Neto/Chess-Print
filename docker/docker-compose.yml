  services:
    mysql:
      image: mysql:8.0
      restart: always
      ports:
        - "3307:3306"
      volumes:
        - mysql_data:/var/lib/mysql
      healthcheck:
        test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
        interval: 5s
        retries: 10

    next:
      build:
        context: ../application
      ports:
        - "3000:3000"
      depends_on:
        - mysql
        - fastapi
      volumes:
        - ../application/src/app:/app/src/app
        - ../application/database:/app/database
        - ../application/public:/app/public
      entrypoint: ["/app/entrypoint.sh"]
      environment: 
        DOCKER_HOST: mysql
        FASTAPI_ENDPOINT: http://fastapi:8000/convert/

    fastapi:
      build:
        context: ../API
        dockerfile: Dockerfile
      ports:
        - "8000:8000"
      volumes:
        - ../API/src:/app/src
      environment:
        FASTAPI_ENDPOINT: http://fastapi:8000/convert/
  
    
  volumes:
    mysql_data:
